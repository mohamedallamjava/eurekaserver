spring:
  application:
    name: eureka-server
#  config:
#    import-check:
#      enabled : false
  cloud:
    config:
       uri: http://localhost:8071
    loadbalancer:
        ribbon:
          enabled : false

server:
  port: 8070